<LumexAlert Title="This is a dismissible alert" 
            Color="ThemeColor.Warning"
            OnClose="@(() => IsAlertClosed = true)"
/>

@if (IsAlertClosed)
{
    <LumexButton>
        Show Alert Again
    </LumexButton>
}

@code{

    public bool IsAlertClosed { get; set; }

}